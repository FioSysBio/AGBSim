turnOnPrint: true
turnOnDraw: false
userMail: acbm.service@gmail.com
passMail: ypczrmajwtxozsan

server:
  port: 8187
  forward-headers-strategy: framework

spring:
  application:
    description: ACBM service
    name: acbm-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3310/acbm-service?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    url: jdbc:mysql://localhost:3306/acbm-service?useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
#    username: root
    username: acbm
#    password: procc123
    password: Procc@123
    testWhileIdle: true
    validationQuery: SELECT 1
    hikari:
      connectionTimeout: 30000
      idleTimeout: 600000
      maxLifetime: 1800000
      maximum-pool-size: 20
      minimum-idle: 10
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /index.html
    enabled: true
    syntax-highlight:
      activated: false